const { test } = require('./helpers/admin-test');

test.describe.skip('Mermaid config - Schema docs', () => {
	test('covers schema-backed config examples', async () => {
		// Reserved for schema-driven runtime coverage if the editor exposes direct config injection.
	});
});