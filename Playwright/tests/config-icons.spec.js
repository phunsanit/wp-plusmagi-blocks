const { test } = require('./helpers/admin-test');

test.describe.skip('Mermaid config - Icons', () => {
	test('registers an icon pack and renders icon references', async () => {
		// Reserved for a future runtime hook that can call mermaid.registerIconPacks.
	});
});